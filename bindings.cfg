exec traditional_bindings.cfg
// exec competitive_bindings.cfg

bind c menu
bind r reload
bind t reloadnext
bind g dropflag
bind v dropammo
bind b discard
bind m saveme
bind i maphelp
bind o flaginfo
bind p inv
bind , changeclass
bind . changeteam
